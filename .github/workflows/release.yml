name: Release
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  release:
    uses: agoodkind/go-makefile/.github/workflows/_release.yml@main
    with:
      binary: send-email
    permissions:
      contents: write
      id-token: write
      attestations: write
    secrets: inherit
